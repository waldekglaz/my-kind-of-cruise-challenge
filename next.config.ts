import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "www.mykindofcruise.com",
      "images.ctfassets.net",
      "img.cruisecritic.net",
    ],
  },
};

export default nextConfig;
